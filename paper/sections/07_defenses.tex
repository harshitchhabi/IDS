\section{Defenses}
\label{sec:defenses}
% Budget: 1.25 pages. Source: DECISIONS.md s20, s21.5, s23, s24.1, s25.3, s25.5.
% - Policy-consistency blind spot: loss filtering recovers ~0% (s23).
% - D1 as pre-registered; missed its own sensitivity criterion (s20, s21.5).
% - Frontier vs kNN and no-skill baselines; uniform fails at ratio 0.9 (s24.1).
% - ShareCap: undominated in all 87 comparisons; cap curve (s25.3, s25.5).
% - Overhead: D1 ~781x cheaper than kNN; ShareCap near zero.
% - Counter-moves: padding (s21.6), volume (s21.4).
% Writing rule (docs/PAPER_PLAN.md): never say D1 is effective, or that ShareCap "beats"
% others (it is undominated, not shown superior in every comparison).
% TODO: prose.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/F6_frontier.pdf}
  % Source: results/phase0/frontier/frontier_points.csv (D1/D1q/D1fixed/kNN/loss/uniform
  % recovery_r0.5, recovery_r0.9, retention_* columns -- only D1 E*=8 g=2 (pre-registered),
  % D1q q=0.99 g in {1,2}, kNN, uniform w in {0.05,0.1} have recovery_r0.9 recorded) +
  % results/phase0/loop/{cicids_recal,synthetic}/jobs/*sharecap_c*.csv (full ShareCap cap
  % curve at both ratios, computed fresh in experiments/paper_figures.py:_sharecap_curve --
  % DECISIONS.md s25.5, independent of the three-cap subset in frontier_points.csv).
  % Regenerated by: python scripts/run.py figures --only F6
  \caption{Recovery of A1 damage vs.\ retention of the honest loop's S0 gain, three retention
  axes (columns), at poison ratio 0.5 (top row) and 0.9 (bottom row; only configurations run at
  that ratio are plotted). Points: D1 variants, kNN sanitize, loss filter, fixed-weight uniform
  down-weighting (dotted: the no-skill line at each weight). Orange: ShareCap's full
  cap-sensitivity curve, $c\in\{0.01,\ldots,0.5\}$, with $c$ labeled at three points. Circled:
  the pre-registered D1 configuration ($E^*=8,\gamma=2$). Insets zoom the crowded high-retention
  corner of the top-row synthetic panels; the main axes keep their full 0--100\% range, so the
  pre-registered D1 point stays visible outside the zoomed region. Uniform fails at ratio 0.9
  (recovery 43--52\%) where ShareCap does not; at ratio 0.5 alone they look similar.}
  \label{fig:f6-frontier}
\end{figure*}

\begin{table}[t]
  \centering
  \caption{TODO: loss-filter recovery by ratio. Source: DECISIONS.md s23.}
  \label{tab:loss-filter}
  \begin{tabular}{lrr}
    \toprule
    Poison ratio & Recovery & Notes \\
    \midrule
    TODO & TODO & TODO \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{table*}[t]
  \centering
  \caption{TODO: defense comparison. Source: DECISIONS.md s24.1, s25.3.}
  \label{tab:defense-comparison}
  \begin{tabular}{lrrrrr}
    \toprule
    Defense & Recovery r0.5 & Recovery r0.9 & Retention CICIDS & Retention synthetic & Overhead / round \\
    \midrule
    TODO & TODO & TODO & TODO & TODO & TODO \\
    \bottomrule
  \end{tabular}
\end{table*}
