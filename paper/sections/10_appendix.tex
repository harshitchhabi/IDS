\section{Appendix}
\label{sec:appendix}
% Source: DECISIONS.md s1 (day-split robustness), s14 (burst splitting), s11 (guard (a) grid
% sweep), s20 + s24 (D1 definition and full grid), s25.6 (XGBoost per-seed trajectories),
% s21.6 (padding), s25.5 (full cap curve -- already in Figure~\ref{fig:f6-frontier}, main body).
% TODO: prose, and decide which tables/figures move here vs. stay in the main body once the
% page budget is known (docs/PAPER_PLAN.md "Confirm the venue, template and page limit").

\subsection{Median realized distance: the superseded capability axis}
% Source: DECISIONS.md s26.2/26.3. Kept because median still separates the far end of the grid
% (twin fraction is uniformly 0 there) even though it cannot resolve the cliff (Section 6).
\begin{figure*}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/F3b_damage_vs_median_distance_appendix.pdf}
  % Regenerated by: python scripts/run.py figures --only F3b (experiments/paper_figures.py:fig_f3_median_appendix)
  \caption{The same A1 damage-vs-distance data as Figure~\ref{fig:f3-damage-vs-fidelity}, plotted
  against median realized mimicry distance instead of twin fraction. This was this paper's
  original capability axis; it still separates the far end of the grid (e.g. jitter 0.03 from
  jitter 1.5), where twin fraction is uniformly zero, but it cannot resolve the FPR cliff itself
  (Section~\ref{sec:attack}): the three leftmost points at ratio 0.2/0.5 (jitter 0, 0.002, 0.005)
  nearly coincide on this axis, yet two are catastrophic and one is inside the noise floor.}
  \label{fig:f3b-median-appendix}
\end{figure*}

\subsection{Day-split robustness}
% TODO.

\subsection{Burst splitting}
% TODO.

\subsection{D1: full definition and grid}
% TODO. Source: DECISIONS.md s20, s24.

\subsection{XGBoost per-seed trajectories}
% TODO. Source: DECISIONS.md s25.6.

\subsection{Cost padding}
% TODO. Source: DECISIONS.md s21.6.
